// JavaScript to trigger animation once on page load
document.addEventListener('DOMContentLoaded', () => {
    const textAnimation = document.querySelector('.text-animation');
    textAnimation.classList.add('play-animation');
});
